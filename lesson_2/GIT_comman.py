'''
git init
git clone URL - Download Master
git checkout -b "name branch" - new your branch
git add 'files' - додали файли у коміт
git commit -m 'add tests' - додаємо коміт
git push origin branch_name - відправляємо на сервер Branch
git pull origin master - if on Master are new changes
git status - show files that is not in git
'''